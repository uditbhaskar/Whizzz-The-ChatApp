package com.example.whizzz

import org.junit.Assert.assertEquals
import org.junit.Test

/**
 * Placeholder JVM unit test module entry (replace with real tests over time).
 *
 * @author udit
 */
class ExampleUnitTest {
    @Test
    fun addition_isCorrect() {
        assertEquals(4, 2 + 2)
    }
}
