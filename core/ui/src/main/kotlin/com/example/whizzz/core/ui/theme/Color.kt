package com.example.whizzz.core.ui.theme

import androidx.compose.ui.graphics.Color

/**
 * Primary brand purple for buttons, links, and key accents in light/dynamic schemes.
 * @author udit
 */
val WhizzzPurple = Color(0xFF6B4EE6)

/**
 * Darker purple for secondary emphasis and pressed states.
 * @author udit
 */
val WhizzzPurpleDark = Color(0xFF4A32A8)

/**
 * Default app scaffold background (dark shell for chats and home).
 * @author udit
 */
val WhizzzScreenBackground = Color(0xFF16191C)

/**
 * Elevated surface / muted panel fill on top of [WhizzzScreenBackground].
 * @author udit
 */
val WhizzzSurfaceMuted = Color(0xFF242325)

/**
 * Bright mint accent (online dots, sender bubbles, highlights).
 * @author udit
 */
val WhizzzAccent = Color(0xFF63FFA3)

/**
 * Secondary mint variant for complementary highlights.
 * @author udit
 */
val WhizzzAccentSecondary = Color(0xFF63FF90)
